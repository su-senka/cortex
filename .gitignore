# ── Build output ──────────────────────────────────────────────────────────────
bin/
obj/
/packages/
*.tsbuildinfo

# React build output (rebuilt by `npm run build` / dotnet publish / Docker)
src/RagAssistant.Web/ClientApp/node_modules/
src/RagAssistant.Web/wwwroot/

# ── Runtime data ──────────────────────────────────────────────────────────────
*.db
*.db-shm
*.db-wal
src/RagAssistant.Web/rag_metadata.json
dp-keys/

# Connector sync caches (mirrored from remote sources on startup/webhook)
docs/ado-sync/
docs/github-sync/

# ── Secrets ───────────────────────────────────────────────────────────────────
# Generated by start-codespaces.sh / copied from .env.example — never commit
.env

# Self-signed TLS certs generated by scripts/gen-certs.sh
nginx/certs/

# USB packaging output (large binaries)
deploy/

# ── IDE / tooling ─────────────────────────────────────────────────────────────
/.idea/
/.vs/
/.vscode/
riderModule.iml
/_ReSharper.Caches/
*.user
.claude/

# ── OS ────────────────────────────────────────────────────────────────────────
.DS_Store
