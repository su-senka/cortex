# Local development overrides.
# Usage: ./start-local.sh
#
# Keycloak is reachable in the browser at http://localhost:8180, so PublicOrigin
# rewrites the internal Docker hostname to that address for browser redirects.

services:
  app:
    environment:
      Oidc__PublicOrigin: http://localhost:8180
